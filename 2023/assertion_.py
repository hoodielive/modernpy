def avg(marks):
    assert len(marks) != 0
    return round(sum(marks) / len(marks), 2)

seml_marks = [62, 65, 75]
print(f'Average marks for semester 1: avg{(seml_marks)}')

ranks = []

print(f'Average of marks for semester 1: avg{(ranks)}')



